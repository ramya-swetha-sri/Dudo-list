# Production Frontend Configuration
# Set the backend API/Socket.io URL to your deployed server
# For Vercel + external backend (e.g., Render, Railway, Heroku), use your server's URL
# Make sure the server has proper CORS and Socket.io settings for your domain
VITE_API_URL=https://your-backend-url.example.com
